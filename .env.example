# =============================================================================
# Rome Agent Trader — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit .env to git. It contains your private keys and API keys.
# =============================================================================

# --- Solana Wallet ---
# Your bot's trading wallet private key (base58 encoded)
# Generate a NEW wallet just for the bot. Never use your main wallet.
WALLET_PRIVATE_KEY=

# --- Helius (Solana RPC + Webhooks) ---
# Sign up at https://helius.dev
HELIUS_API_KEY=
HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY_HERE

# --- Birdeye (Token Data) ---
# Sign up at https://birdeye.so
BIRDEYE_API_KEY=

# --- Telegram Bot ---
# Create a bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=
# Your personal Telegram chat ID (the bot sends alerts here)
# Get it by messaging @userinfobot on Telegram
TELEGRAM_CHAT_ID=

# --- Trading Parameters ---
# How much SOL to spend per copy trade (start small!)
DEFAULT_POSITION_SIZE_SOL=0.01
# Maximum SOL to spend on a single token across all buys
MAX_POSITION_SIZE_SOL=0.05
# Maximum number of open positions at once
MAX_OPEN_POSITIONS=10
# Maximum SOL loss per day before the bot pauses trading
MAX_DAILY_LOSS_SOL=0.5
# Slippage tolerance for Jupiter swaps (in basis points, 100 = 1%)
SLIPPAGE_BPS=300
# Priority fee for faster transaction inclusion (in microlamports)
PRIORITY_FEE_MICROLAMPORTS=50000

# --- Safety Thresholds ---
# Minimum USD liquidity required before buying a token
MIN_LIQUIDITY_USD=10000

# --- Market Cap Ranges ---
# DISCOVERY: For finding past winners to identify smart wallets ($1M-$50M)
DISCOVERY_MIN_MCAP_USD=1000000
DISCOVERY_MAX_MCAP_USD=50000000
# COPY TRADING: For following smart wallets into new tokens
# Low floor because smart wallets buy early ($50K+). Liquidity check protects us.
MIN_COPY_TRADE_MCAP_USD=50000
MAX_COPY_TRADE_MCAP_USD=100000000
